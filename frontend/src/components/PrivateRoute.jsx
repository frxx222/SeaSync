// import { Navigate } from 'react-router-dom';
// import { useSelector } from 'react-redux';  // Assuming role info is stored in Redux

// const PrivateRoute = ({ children, requiredRole }) => {
//   const userRole = useSelector((state) => state.auth.role);  // Assuming user role is saved in auth state

//   if (userRole !== requiredRole) {
//     return <Navigate to="/dashboard" replace />; // Redirect to dashboard if the role doesn't match
//   }

//   return children;
// };

// export default PrivateRoute;
